boolean[] keys = new boolean[4];

void setup(){
   for(int i = 0 ; i < keys.length ; i++){
     keys[i] = false;
   }
   size(1920, 1080);
   background(255);
   
   
}

void draw(){
  noStroke();
  fill(255);
}
